# 02 — Theme Filter Chips

**What to build:** Filter chips above the grid, auto-derived from the theme values present in the mock data (plus an "ALL" chip). Clicking a chip instantly filters the visible grid to that theme, client-side, no reload.

**Blocked by:** 01 — Foundation & Library Grid

**Status:** ready-for-agent

- [ ] Chip list is derived from the data (no hardcoded theme list) — adding an inspiration with a new theme automatically produces a new chip
- [ ] "ALL" chip shows the full set and is selected by default
- [ ] Selecting a chip filters the grid instantly, client-side, no page reload
- [ ] Selected chip has a distinct visual state per `DESIGN.md` pill/tag component tokens
- [ ] Verified running in browser with the 3 starter inspirations